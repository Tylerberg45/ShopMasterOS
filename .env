# database (defaults to SQLite if not set)
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/oilchange

# Set environment to development for local SQLite usage
APP_ENV=dev

# admin security (optional: protects /admin/* routes)
# ADMIN_TOKEN=changeme-super-secret

# license plate lookup (optional)
PLATE_LOOKUP_PROVIDER=none
PLATE_LOOKUP_KEY=
PLATE_LOOKUP_REGION=US

# Debug settings for local development
DEBUG=true