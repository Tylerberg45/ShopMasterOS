# Railway Environment Variables Configuration
# Copy these into your Railway dashboard under Variables

# Application Settings
PYTHONPATH=/app
PORT=8000

# Database Configuration
# For SQLite (free tier)
DATABASE_URL=sqlite:///./oilchange.db

# For PostgreSQL (when you add Railway's database)
# DATABASE_URL will be automatically set by Railway

# Optional: Application Environment
ENVIRONMENT=production
DEBUG=false
